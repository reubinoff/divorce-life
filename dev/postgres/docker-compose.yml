version: '3.6'

services:
  icsra: 
    image: postgres
    stdin_open: true
    tty: true
    environment:
      POSTGRES_PASSWORD: 'example'
      POSTGRES_USER : 'example'
      PGDATA: '/tmp/'
    ports:
      - "5432:5432"

